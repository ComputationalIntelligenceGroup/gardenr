---
output:
  md_document:
    variant: markdown_github
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, echo = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "README-"
)
```

# gardenr

The goal of gardenr is to ...

## Example


```{r example}
library(gardenr)
annotations <- get_all_labels('/home/bmihaljevic/code/gardener-experiment-data/data/') 
summary(annotations)
``` 

## Path to the data

The user needs to provide the path to the folder containing the data. These can be downloaded from ...(TODO)
